## Nova Assist v1.2

アプリ起動時に黒い画面で停止しダッシュボードが表示されない不具合を修正したパッチリリースです。

### 変更点
- **アプリ自動認証の復旧**: サーバー認証側で合言葉（`?key=` クエリパラメータ）による永続セッションCookieの自動発行・リダイレクト処理を復旧し、初回アクセス時でも確実にダッシュボードが表示されるよう修正
- **HTTP認証ヘッダーの自動付与**: アプリのWebViewアクセス時に `X-Access-Key` ヘッダーを直接注入し、Cookieの有無に依存しない多重認証に対応
- **静的アセットのCORS対応**: スクリプト読み込み時のCORSエラーを防止するため、全静的ファイルレスポンスに `Access-Control-Allow-Origin: *` を適用
- **WebViewエラーハンドリング & リモートデバッグ**: WebViewのデバッグ有効化およびコンソール・HTTP通信エラーログの収集に対応
